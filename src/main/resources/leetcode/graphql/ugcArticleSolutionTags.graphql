query ugcArticleSolutionTags($questionSlug: String!) {
  ugcArticleSolutionTags(questionSlug: $questionSlug) {
    otherTags {
      name
      slug
      count
    }
    knowledgeTags {
      name
      slug
      count
    }
    languageTags {
      name
      slug
      count
    }
  }
}
