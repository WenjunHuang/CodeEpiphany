#if($challenge.slug)
    $!velocity.camelCaseName($challenge.slug)
#elseif($challenge.name)
    $!velocity.camelCaseName($challenge.name)
#elseif($challenge.title)
    $!velocity.camelCaseName($challenge.title)
#else
    "Unknown"
#end